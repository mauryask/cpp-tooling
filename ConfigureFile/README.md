This project deals with the auto-generation of the header file
It generates a configuration file called configuration.hpp 
it contains project name, project version and version (major, minor and patch)